CREATE PROCEDURE sp_Unidades_CRUD
    @Action CHAR(1),        -- 'L','O','C','U','D'
    @Id     INT     = NULL,
    @WarehouseId INT = NULL,
    @PeriodicConsumptionId INT = NULL
AS
BEGIN
    -- Implementación CRUD de unidades
END
