CREATE PROCEDURE RasurarTickets
    @TicketId INT = NULL,
    @TicketIds XML = NULL,
    @MontoObjetivo DECIMAL(18, 2) = 0,
    @Debug BIT = 0
AS
BEGIN
    SET NOCOUNT ON;
    -- Lógica de rasurado de tickets
END
